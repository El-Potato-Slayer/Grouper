<%= content_for(:title, "All Expenses") %>
<h1 class="text-center text-3xl py-4 bg-white"><%= yield(:title) %></h1>

<%= render "total_expenses" %>

<%= render partial: "recent_expenses", locals: { expenses: @expenses.includes([:group_expenses])} %>

<%= render "shared_partials/add_button" %>
