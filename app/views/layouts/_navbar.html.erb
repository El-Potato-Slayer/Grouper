<nav >
  <div class="bg-blue-500 flex justify-between p-2">
    <button id="nav-btn" class="text-white rounded nav-toggler" >
      <i  class="material-icons">menu</i>
    </button>
    <p class="text-lg text-white uppercase ">Expenses</p>
    <i class="material-icons text-white">search</i>
  </div>
  <div id="nav-links" class="hidden" >
    <div class="flex flex-col items-center text-center">
      <%= link_to "Expenses", :root, class: "py-1 w-full" %>
      <%= link_to "External expenses", external_expenses_index_path, class: "py-1 w-full" %>
      <%= link_to "Groups", groups_path, class: "py-1 w-full" %>
      <% if user_signed_in? %>
        <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: "py-1 w-full" %>
      <% else %>
        <%= link_to 'Login', new_user_session_path, class: "px-3 py-1" %>
        <%= link_to 'Register',  new_user_registration_path, class: "px-3 py-1" %>
      <% end %>
    </div>
  </div>
</nav>